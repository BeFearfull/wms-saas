spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/wms_db_dev
    username: postgres
    password: password
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

jwt:
  secret: dev-secret-key-only-for-development-purposes-change-in-production
  expiration: 604800000 # 7 days for dev

logging:
  level:
    root: DEBUG
    com.wms: DEBUG
    org.springframework.security: DEBUG